/* Q1
todo: (Change skills array to JSON using JSON.stringify()) */

let jsonToArray = JSON.stringify(skills);
console.log(jsonToArray);

/* Q2
todo: (Stringify the age variable) */

let strAge = JSON.stringify(age);
console.log(strAge);

/* Q3
todo: (Stringify the isMarried variable) */

let strIsMarried = JSON.stringify(isMarried);
console.log(strIsMarried);

/* Q4
todo: (Stringify the student object) */

let strObj = JSON.stringify(student)
console.log(strObj)